# Blender MCP add-on (moved)

The Blender extension source now lives at [`mcp/blender_mcp_addon/`](../mcp/blender_mcp_addon/).

Build and install from the repo root:

```bash
blender --command extension build --source-dir ./mcp/blender_mcp_addon
```
